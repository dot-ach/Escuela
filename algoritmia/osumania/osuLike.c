#include <stdio.h>

int main()
{
  int cantFilas;
  // char beatmapActual[100];
  int i, j;

  i = 0;
  // beatmapActual = "#...";

  scanf("%d", &cantFilas);

  while (i < cantFilas)
  {
    char beatmap[cantFilas][100];
    // while (j < )
    scanf("%s", beatmap);

    i++;
  }
    // printf("%s", beatmapActual);

  return 0;
}