#include <stdio.h>
#include "conjunto.h"

int main()
{
  // conjunto prueba;
  // int i;

  // InicializarConjunto(&prueba);
  // for(i = 1; i <= 10; i++)
  // {
  //   AgregarElemento(&prueba, i);
  // }

  // ImprimeConjunto(&prueba);

  // EliminarElemento(&prueba, 2);
  // ImprimeConjunto(&prueba);

  // int num, i, primo;
  // primo = -1;

  // scanf("%d", &num);

  // for(i = 1; i <= num; i++)
  // {
  //   if(num % i == 0 && i != 1 && i != num)
  //   {
  //     primo = 0;
  //   }
  // }

  // if(primo == 0)
  // {
  //   printf("No es primo");
  // }else
  // {
  //   printf("Es primo");
  // }

  printf("%d",EsPrimo(1));


  return 0;
}